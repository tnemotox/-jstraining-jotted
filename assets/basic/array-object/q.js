// ボタンがクリックされたときの処理
$('#check').on('click', function () {
    'use strict';
    // 以下に解答を記述する
    // オブジェクトを1つ格納した配列employeeを宣言する。
    // オブジェクトの形式は、{ name: 'name', age: 1 } とする
    var employee;

    // personと同じ構造のオブジェクトを2つ宣言する

    // 上で宣言した2つのオブジェクトを、順に配列の末尾に追加し、配列をコンソールに出力する
    
    console.log(employee);
    
    // 配列宣言時に格納した、一番先頭のオブジェクトを配列から削除し、配列をコンソールに出力する

    console.log(employee);
});